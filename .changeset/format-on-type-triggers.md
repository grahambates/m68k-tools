---
"m68k-lsp-server": patch
---

Trigger format-on-type on `#`, `:`, `=` and `)` in addition to the existing trigger characters, so immediate-mode operands, labels, direct assignments and closed addressing modes are reformatted as you type them.
