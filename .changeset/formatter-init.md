---
"m68k-formatter": minor
---

Add `m68k-format --init` to create a `.m68k-format.json` config file interactively, asking for case, label colons, quote style, operand spacing, indent style, trailing whitespace, final newline and line endings. Only answers that differ from the defaults are written.
