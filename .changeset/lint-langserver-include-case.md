---
"m68k-lint-langserver": patch
---

Report `portability/include-case` in the editor: an include whose case differs from the file on disk, with the path as it is on disk offered as a quick fix. Only computed when the rule is on.
