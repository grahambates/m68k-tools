{
  "name": "@m68k-lsp/assembly-options",
  "version": "0.0.0",
  "private": true,
  "description": "Shared options for how a project's source is assembled: read from .m68krc.json and vasm arguments",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run --config ../../vitest.package.config.mts",
    "typecheck": "tsc -p tsconfig.test.json",
    "clean": "rm -rf out dist .tsbuild coverage"
  },
  "devDependencies": {},
  "license": "MIT"
}
