{
  "name": "@m68k-lsp/workspace-files",
  "version": "0.0.0",
  "private": true,
  "description": "Shared assembly workspace file discovery and exclude-pattern matching",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "dependencies": {
    "minimatch": "^10.0.1"
  },
  "scripts": {
    "test": "vitest run --config ../../vitest.package.config.mts",
    "typecheck": "tsc -p tsconfig.test.json",
    "clean": "rm -rf out dist .tsbuild coverage"
  },
  "devDependencies": {},
  "license": "MIT"
}
